# 本地练习 profile：保留 HTTP 模拟入口，跨服务 HMAC 只允许由运行环境注入。
# 修改用途：移除提交态固定 HMAC；MQTT 与 Core 上下文补链仍沿用 application.yml 的默认关闭策略。
iot:
  telemetry:
    # 本地练习允许通过 HTTP 模拟 MQTT 摄取；基础 application.yml 默认仍保持关闭。
    simulation:
      enabled: true
