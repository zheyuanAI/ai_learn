# Spec

## Requirement: <name>

### Scenario: <name>

- Given ...
- When ...
- Then ...

## Existing Specs To Update

- 当前仓库示例：`docs/specs/<domain>/{概述,领域模型,接口契约,验收标准}.md`
- 通用 OpenSpec 模板示例中的 `openspec/...` 仅表示通用目录占位，不是本仓库的现行入口；本仓库工件入口为 `docs/openspec/...`。
