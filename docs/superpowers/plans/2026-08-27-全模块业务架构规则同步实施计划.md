# 全模块业务架构规则同步 Implementation Plan

> **For agentic workers:** 按当前会话直接执行；本仓库禁止 AI Git Commit。步骤使用复选框记录完成情况。

**Goal:** 将库存、销售、采购、生产质检、IoT、GIS 看板和 AI 助手的已确认一期规则同步到现有交互式业务架构空间，并完成静态与浏览器验证。

**Architecture:** 保留 `docs/业务架构/` 现有九个 HTML 页面和数据驱动渲染结构；规则内容集中维护在 `business-architecture-data.js`，公共可信度和评审提示由 `business-architecture.js` 渲染。同步更新原设计记录中的旧“待专项确认”描述，不改原型页面映射或前端 Vue 路由。

**Tech Stack:** 语义化 HTML5、原生 CSS、原生 JavaScript、静态 HTTP 服务、Playwright。

---

### Task 1: 同步七模块确认规则

**Files:**
- Modify: `docs/业务架构/assets/business-architecture-data.js`
- Modify: `docs/业务架构/assets/business-architecture.js`

- [x] **Step 1:** 将七个模块可信度统一改为 `confirmed（已确认）`，更新预览页为“七模块规则已确认、等待整体复核”。
- [x] **Step 2:** 按正式业务规则补齐库存盘点、采购质量处置、生产工单审核、IoT 告警、GIS 刷新与 AI 中转站边界。
- [x] **Step 3:** 更新黄金闭环节点和总览事实边界，确保模块页与总览使用同一数据源。
- [x] **Step 4:** 使用 `node --check` 检查两个 JavaScript 文件。

### Task 2: 同步设计与入口说明

**Files:**
- Modify: `docs/superpowers/specs/2026-08-27-业务架构空间与销售业务架构图设计.md`
- Modify: `docs/superpowers/plans/2026-08-27-业务架构交互HTML实施计划.md`
- Modify: `docs/prototype/README.md`

- [x] **Step 1:** 清除“销售已确认、其他模块待确认”的过期描述，改为七模块均已确认。
- [x] **Step 2:** 明确当前下一步是用户整体复核，复核通过后才进入其余高保真原型。
- [x] **Step 3:** 检查相关入口、状态说明和空间跳转仍准确。

### Task 3: 静态与浏览器验收

**Files:**
- Verify: `docs/业务架构/**`
- Verify: `docs/specs/**`

- [x] **Step 1:** 检查过期文案、九个页面入口、七个模块编码、相对链接和 Git 空白错误。
- [x] **Step 2:** 启动 `docs` 静态服务器，验证预览、总览五视图和七个模块页可打开且控制台无错误。
- [x] **Step 3:** 在桌面和 768px 视口检查页面横向溢出、模块导航、节点详情和空间跳转。
- [x] **Step 4:** 关闭临时服务器，保留验证证据供交付说明使用。
