# 工作区根目录与参考工程规则迁移实施计划

> **已废止（历史追溯）：** 本文仅记录 2026-08-27 的迁移过程，不代表当前项目方案。当前工作区、数据库和版本基线以 `AGENTS.md`、`CLAUDE.md` 及 `docs/specs/00-project/` 正式规格为准。

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将旧工作区中的有效目录边界和阅读导航融合到当前开发项目，同时明确 `ai_learn_referenceProjects` 仅为只读学习参考目录。

**Architecture:** 当前项目根目录继续作为唯一开发和事实解析根。规则文件负责约束目录边界，README 文件负责解释入口和阅读路径；外部参考目录只在文档中被指向，不纳入当前项目代码、配置或实现事实。

**Tech Stack:** Markdown、PowerShell、Git

---

### Task 1: 同步根目录代理规则

**Files:**
- Modify: `AGENTS.md`
- Modify: `CLAUDE.md`

- [x] **Step 1: 在两个规则文件的项目概览后增加工作区边界**

加入以下内容；`CLAUDE.md` 保留自身标题，其他规则文字保持一致：

```markdown
## 工作区边界

- 当前开发项目根目录为 `D:\AI\ai_learn_wms_ai\ai_learn_developProject`；项目代码、配置、规格、原型、测试和运行说明均以此目录为根解析。
- `D:\AI\ai_learn_wms_ai` 是旧工作区容器，不是当前开发项目根目录；禁止把其中的工作区级文件或历史路径当作当前项目入口。
- `D:\AI\ai_learn_wms_ai\ai_learn_referenceProjects` 是学习参考目录，仅供只读阅读、对照和学习；不得把其中的代码、配置、端口或版本表述为当前项目已实现事实，也不得修改其中内容。
- 参考工程与当前项目的运行基线相互隔离；参考工程可以继续使用自身的 Node 22 与 PostgreSQL 12.1 兼容配置（端口 5323）等配置，不能据此反推当前项目配置。
- 如果其他机器或运行环境找不到上述外部路径，必须明确写“未在仓库中确认”，不得仅凭目录名推断其内容。
```

- [x] **Step 2: 清理规则文件中对未保留模板文件的悬空引用**

将 `AGENTS.md` 联动检查中的旧通用规则模板引用改为当前实际生效的 `AGENTS.md`、`CLAUDE.md` 和根 `README.md`，不新增该模板文件或单独的 `docs/README.md`。

- [x] **Step 3: 检查规则同步结果**

Run:

```powershell
rg -n -F '工作区边界' AGENTS.md CLAUDE.md
rg -n -F 'D:\AI\ai_learn_wms_ai\ai_learn_referenceProjects' AGENTS.md CLAUDE.md
rg -n -F '仅供只读阅读' AGENTS.md CLAUDE.md
```

Expected: 三条命令均在两个文件中找到对应规则或边界说明。

### Task 2: 更新项目入口 README 与目录说明

**Files:**
- Modify: `README.md`

- [x] **Step 1: 在根 README 增加工作区定位**

在项目简介后加入“工作区定位”说明，明确当前项目根目录、旧工作区容器和只读参考目录；同时在阅读入口中增加参考工程仅用于学习对照的说明。

- [x] **Step 2: 不保留单独的 `docs/README.md`**

当前项目文档与外部参考目录的导航统一放入根 `README.md`；不新增或恢复单独的 `docs/README.md`。

- [x] **Step 3: 在根 README 的“文件结构”章节增加项目外部工作区说明**

在根 README 增加“工作区定位”和“文档事实边界”说明，明确当前文件只描述 `ai_learn_developProject`，并将外部参考目录标记为只读学习资源；不恢复已删除的历史文档或旧通用规则模板。

- [x] **Step 4: 检查 README 导航结果**

Run:

```powershell
rg -n -F 'D:\AI\ai_learn_wms_ai\ai_learn_developProject' README.md
rg -n -F 'D:\AI\ai_learn_wms_ai\ai_learn_referenceProjects' README.md
```

Expected: 两条命令均能找到当前根目录和参考目录说明。

### Task 3: 迁移范围与文档质量验证

**Files:**
- Test: `AGENTS.md`
- Test: `CLAUDE.md`
- Test: `README.md`

- [x] **Step 1: 确认没有创建被排除的模板文件**

Run:

```powershell
Test-Path -LiteralPath (Join-Path 'docs' (Join-Path 'rules' 'AGENTS_General.md'))
```

Expected: `False`。

- [x] **Step 2: 检查 Markdown 和 Git 空白错误**

Run:

```powershell
git diff --check -- AGENTS.md CLAUDE.md README.md
```

Expected: 命令无输出且退出码为 `0`。

- [x] **Step 3: 审阅本次文件范围并保留既有改动**

Run:

```powershell
git status --short
git diff -- AGENTS.md CLAUDE.md README.md
```

Expected: 本次新增或修改内容只涉及工作区边界、参考目录定位、README 导航和悬空引用清理；其他已有未提交改动保持原样；不执行 Git commit。

### Task 4: 合并目录入口并清理后续路径残留

**Files:**
- Modify: `README.md`
- Modify: `docs/openspec/README.md`
- Modify: `backend/README.md`
- Modify: `frontend/README.md`
- Modify: `AGENTS.md`
- Modify: `CLAUDE.md`
- Modify: `docs/superpowers/` 中引用已删除入口的历史设计与实施记录

- [x] **Step 1: 将项目目录说明统一收归根 README**

根 README 保留唯一项目级目录树、文档导航、阶段决策入口、关键配置和工作区边界；不再保留单独的 `docs/README.md`。

- [x] **Step 2: 修正当前根目录下的 OpenSpec 命令入口**

从当前项目根目录执行 OpenSpec 命令时使用 `cd docs`；OpenSpec 子目录树仍只描述 `docs/openspec/` 自身。

- [x] **Step 3: 清理已删除入口的历史文字**

历史设计和实施记录保留迁移事实，但不再把已删除的目录说明、历史学习计划或未保留规则模板写成当前入口，也不创建这些文件。

- [x] **Step 4: 收敛重复摘要并补齐活动导航**

补充 `阶段决策与续聊入口.md`，将模块 README 的环境说明指向架构事实源，并统一 `AGENTS.md` 与 `CLAUDE.md` 的规则文字。
