# 黄金业务闭环浏览器实测问题记录

## 本轮结论

- 测试时间：2026-09-10 13:50–20:46（Asia/Shanghai）。
- 状态：**31 步页面业务动作已走到销售订单“已完成 / 全部发货”，但黄金业务闭环整体验收不通过。** 销售来源、工单采购来源、IoT 设备事实和最终跨域追溯仍断链。
- 已通过真实浏览器页面创建本轮独立数据，完成采购收货、质检放行、上架、生产领料、派工、执行、报工、生产质检、成品入库、工单完工、销售拣货与发货。未通过接口或数据库写入绕过页面。
- 最终销售单 `SO-16f9229f-97fc-4b3b-8863-bce15866da23` 为“已完成 / 全部发货”，订购、已拣、已发均为 100；工单 `WO-9800b5c2-0085-4848-a554-7c77a93448e5` 为“已完工结案”，报工、入库均为 100。
- 2026-09-10 浏览器实测期间只记录问题，未通过代码或数据修改绕过页面。2026-09-11 已按本文“非环境问题处理记录”实施修复，均未提交 Git；测试业务数据继续保留用于复核。

## 依据与测试范围

- 用户指定任务：`codex://threads/01a080cf-a629-7b90-8897-bc2b240b6878`，标题“修复销售流程数据查询问题”。读取该任务最后一份完整的 31 步页面操作脚本及后续成品入库修正说明。
- 仓库约束：`docs/specs/00-project/正式项目计划.md`、`docs/词汇定义表.md`、`docs/prototype/README.md`、ERP/WMS 与 MES 的验收标准。
- 页面：`http://localhost:5173/login?redirect=/sales/orders`，租户 `tenant_demo_a`。
- 实际业务操作只通过浏览器界面执行。端口和配置的只读检查用于定位环境问题，不作为业务步骤执行成功的证据。
- 历史任务的“已完成”结论不算本轮验证结果；本轮没有通过接口调用或数据库写入绕过页面。

## ENV-001：后端服务未监听，登录阻断整个流程

- 严重程度：阻断。
- 类型：本地运行环境问题，尚不能据此认定登录代码存在缺陷。
- 前置：前端登录页可打开，租户下拉为 `tenant_demo_a`。
- 复现：
  1. 点击“租户管理员 admin.zhang”。
  2. 页面提示已选中演示角色并自动填入演示凭据。
  3. 点击“登录进入系统 POST /api/auth/login”。
  4. 按钮短暂显示“正在登录...”，随后恢复，仍留在登录页。
  5. 再次点击登录按钮，结果相同。
- 预期：登录成功并进入有权限的业务页面；服务不可用时应有可识别的失败提示。
- 实际证据：两次操作后的页面地址均保持登录地址，没有进入业务列表。本轮未捕获稳定可见的错误提示，不据此断言系统完全没有 toast。
- 只读环境检查：`Get-NetTCPConnection -State Listen` 显示 5173、5433、6379、1883 正在监听；20001、10002、10003、10004 均未监听。
- 配置证据：`frontend/vite.config.ts` 将 `/api` 代理到 `http://localhost:20001`。
- 判断：本机预期 Gateway 入口未启动，足以阻断真实登录；Auth、Core、IoT 也未在项目规定端口运行。没有取得后端错误码或 HTTP 状态码，不作推测。
- 恢复条件：启动项目后端服务，重新通过页面登录，再继续以下执行清单。

## ENV-002：既有 Redis 监听状态不符合仓库自动复用条件

- 类型：运行约束，不是业务页面缺陷。
- 实际观察：6379 监听地址包含 `0.0.0.0` 和 `::`。
- 依据：`runtime/README.md` 明确要求既有 Redis 仅绑定 `127.0.0.1/::1` 且认证策略一致，经用户验证后才可复用；不得自动停止既有进程。
- 本轮处理：未停止或修改该 Redis，未自行启动依赖它的后端；已请求用户在 IDEA 中恢复服务后继续。

## DOC-001：原脚本成品入库确认角色需要校正

- 类型：历史脚本与后续修正说明不一致，本轮页面已复核确认。
- 原脚本第 27、28 步均使用生产质检人员创建并确认成品入库。
- 同任务后续说明已经明确：`mes.inspector` 可创建入库草稿，但“确认入库”按权限禁用。
- 本轮实际：`mes.inspector` 创建 `FGR-ce75f2ae-b578-42a2-bcc7-d61751f542af` 后，“确认入库”确实禁用；切换 `warehouse.operator` 后可确认并产生库存流水 `d98d5a9b-56e6-4207-820f-278a25d6e827`。正确脚本应明确跨角色执行。

## DOC-002：来源关联与完整黄金闭环覆盖不足

- 类型：脚本验收覆盖问题，非本轮已复现的实现缺陷。
- 原脚本第 11 步允许不知道销售行 ID 时留空。该做法可以验证独立制造流程，但不能证明销售需求到生产的人工供需关联已经闭环。
- 后续应优先使用页面提供的真实来源信息；若页面不能取得或选择销售行，应记录可操作性缺口，不能随机填写 UUID 或用数据库补关联后宣称页面通过。
- 原脚本 31 步主要覆盖主数据、采购、制造和销售，不包含正式计划要求的设备遥测、告警、工序关联与最终跨域追溯验证。应将这些列为补充项，不能仅以销售单完成宣布全部黄金闭环通过。

## 执行清单

| 原脚本步骤 | 内容 | 本轮结果 |
| --- | --- | --- |
| 角色登录 | 六类演示角色切换 | 已执行；服务恢复后可登录，首跳曾出现一次错误 403，见 UI-008 |
| 1–6 | 单位、仓库、六类库位、客户、供应商、原料和成品 | 已完成 |
| 7–8 | BOM、工艺路线 | 已完成并生效 |
| 9–10 | 销售创建、提交、审核 | 已完成，100 件 |
| 11–12 | 关联销售来源创建工单、提交、批准 | 工单已完成创建、提交、批准；销售来源未关联，见 UI-003 |
| 13–14 | 关联工单采购、提交、审核 | 独立采购已完成；关联工单采购被 `PO_004` 拒绝，见 UI-005 |
| 15 | 仓库接收进入 QualityHold | 已完成，100 件 |
| 16–17 | 到货质检、质量处置决定 | 已完成，100 件全部合格并放行 |
| 18–19 | 仓库执行放行、上架 | 已完成，QH → ReceivingStaging → Storage |
| 20 | 生产领料申请、列表回显、仓库出库确认 | 已完成，原料扣减 100 件 |
| 21 | 派工创建、下达 | 已完成，100 件 |
| 22–24 | 执行实例、开始/完成、报工 | 已完成，报工合格 100、不良 0；列表累计显示异常见 UI-011 |
| 25–26 | 生产质检创建、结论 | 已完成，首件检验 100 件全部合格 |
| 27–29 | 成品申请、仓库确认入库、工单完工 | 已完成；质检员创建、仓库员确认，角色校正成立 |
| 30–31 | 销售直接拣货、发货 | 已完成，销售单自动进入“已完成 / 全部发货” |
| 补充 | 设备遥测、告警、工序关联 | 阻断；设备模型为空且页面无模型维护入口，见 UI-012 |
| 补充 | 销售来源、采购来源、库存流水及全链追溯 | 库存流水已产生；两个来源关联和追溯未通过，见 UI-003、UI-005、UI-004 |

## 续测记录要求

恢复后使用新的测试编码后缀 `20260910-UI01`，避免混淆历史 GOLD 链路。逐项记录角色、单号、实际数量、页面状态和复现操作；可绕开的非阻断问题记录后继续，无法绕开的前置问题明确标注后续依赖步骤未验证。保留测试数据用于复核，不清理既有业务记录。

## 服务恢复后的续测（业务动作已完成）

用户确认服务启动后，真实登录已成功，ENV-001 的登录阻断解除。Chrome 页签点击未切换，改用内置浏览器后正常；原因未确认，不计为业务代码缺陷。

已通过页面创建并验证：单位 `PCS-20260910-UI01`（备注回显正常）、仓库 `WH-20260910-UI01`、六类库位 `LOC-{RECV,STOR,PICK,SHIP,QH,ADJ}-20260910-UI01`、客户 `CUS-20260910-UI01`、供应商 `SUP-20260910-UI01`、原料 `RM-20260910-UI01`、成品 `FG-20260910-UI01`。客户和供应商全部填写字段均回显。BOM `BOM-20260910-UI01` 为一件成品消耗一件原料，工艺路线 `RTG-20260910-UI01` 为单道“闭环实测装配”，两者均生效。

- 销售单：`SO-16f9229f-97fc-4b3b-8863-bce15866da23`，100 件，已提交并审核，页面为“履约中 / 尚未开始”。真实订单实体 ID 从页面追溯链接取得：`d1e4361c-1ae7-4335-a265-5f18c324d1de`。
- 工单：`WO-9800b5c2-0085-4848-a554-7c77a93448e5`，100 件，已提交、批准，页面为“已下达排产”；来源销售行留空，原因见 UI-003。

### UI-001：库位列表所属仓库显示缺失

主数据“6类标准库位”列表中，已有及新建库位的“所属仓库”显示 `-`；新建时仓库下拉可正常选择。影响仓管辨认库位归属，未阻断保存。预期列表展示真实仓库名称或编码。

### UI-002：仓库类型出现一期排除的 WIP

新建仓库类型下拉提供 `WIP (线边缓冲仓)`；正式计划明确一期不实现 WIP/线边仓。仅观察选项，未选择或保存此类型。需统一一期页面范围。

### UI-003：销售来源行缺少可操作的选择入口

销售详情明细只显示物料、数量、预留和发货进度，没有销售行 UUID；新建工单只提供“来源销售订单行 ID (可选，仅用于追溯)”文本框，没有销售订单/明细选择器。无法仅凭页面完成来源关联。本轮按原脚本允许留空继续独立工单验证；不得将其表述为销售—制造关联验收通过。

### UI-004：销售全链追溯接口不可用

销售角色在上述销售单详情点击“全链路追溯”，跳转 `/traceability?entry_type=SALES_ORDER&entity_id=d1e4361c-1ae7-4335-a265-5f18c324d1de`。页面显示“追溯链拓扑装载失败”，`CODE: GIS_QUERY_002 请求的静态资源或接口不存在: api/traceability`。追溯不可用；页面附带“跨服务图数据库”文案也不应被当作实际架构事实。暂未判定为实现缺失还是运行开关未开启。

### UI-005：原料采购关联成品生产工单被拒绝

- 角色：采购人员 `buyer.chen`，租户 `tenant_demo_a`。
- 前置：上述工单已下达，目标产出为 `FG-20260910-UI01`，生效 BOM 组件为 `RM-20260910-UI01`。
- 操作：新建采购单，选择本轮供应商、仓库、原料 `RM-20260910-UI01`、100 件、来源工单 `WO-9800b5c2-0085-4848-a554-7c77a93448e5`，点击“确认生成采购单”。
- 实际：浏览器 alert 和页面均报 `PO_004 来源生产工单不存在、已删除、跨租户或产品不一致`；请求号 `req-a93d9dee-b654-4598-b5cf-1aac86b2a7a8`。表单保留，未成功生成单据。
- 预期：同租户有效工单所需 BOM 原料可以建立采购来源关联。
- 影响：黄金闭环的生产—原料采购关联阻断。错误的具体内部校验原因尚未确认，不能仅据文案断定数据库数据损坏。
- 续测方式：取消可选工单关联，再测试独立采购与库存执行；该绕行不计为来源关联通过。

### ENV-003：执行质量放行时后端服务中途全部退出

- 严重程度：阻断，且本次写操作结果不确定。
- 已完成前置事实：取消来源工单关联后成功创建采购单 `PO-afe78c33-26de-4be2-a613-bb588a54993b`，完成提交和审核；仓库人员接收 100 件到 `LOC-QH-20260910-UI01`，服务端返回收货凭证 `f0872231-6da8-30da-a980-5805ca5083b3`；生产质检人员成功保存检验事实 `QI-821bc445-b5b6-4e5b-945a-1bb83bbffa1c`，100 件全部合格。
- 复现时间：2026-09-10 20:11（Asia/Shanghai）。
- 触发操作：对上述质检记录选择“合格放行上架”，数量 100，填写原因后点击“确认下达处置决定”。
- 实际：按钮进入禁用状态，随后浏览器 alert 显示“无法连接至后端网关服务，操作结果不确定 (Network Error)”。
- 环境复核：20001、10002、10003、10004 均无监听；没有发现 `java`、`javaw` 或 `idea64` 进程。前端页面仍可打开。
- 风险：不能断言放行决定未写入。服务恢复后必须先刷新“质量处置决定与执行”列表，以检验号 `QI-821bc445-b5b6-4e5b-945a-1bb83bbffa1c` 查询是否已有决定；只有确认不存在时才能重试，避免重复产生业务事实。
- 未验证依赖步骤：质量执行、上架、生产领料、派工与执行、报工、生产质检、成品入库、工单完工、销售拣货发货、IoT 告警关联和最终追溯。
- 恢复结果：重新启动服务后先刷新处置列表，确认该次不确定请求没有生成决定；随后重新下达 `QD-8435c6cc-13c4-475c-94b9-385d516a897d`，没有产生重复处置，依赖业务步骤均继续执行。IoT 和追溯仍因独立问题未通过。

### UI-006：收货与质检列表关键主数据仍显示 UUID 或空值

- 采购列表的供应商、目标仓库显示为空或 `-`；采购详情的首行物料显示 `-`。
- 到货验收明细把物料 SKU/名称显示成重复的产品 UUID `ad52226c-4e4f-4660-8166-246b6d544046`。
- 质检列表列名为“物料 UUID”，并显示原始 UUID；质检员也显示原始用户 UUID。
- 影响：不阻断业务提交，但仓库与质检人员无法从列表直接辨认真实业务对象，容易选错记录。

### UI-007：到货验收默认时间按 UTC 显示

- 本机时区为 Asia/Shanghai，打开到货验收弹窗时约为本地 14:45，但“到货验收时间”默认显示 `2026-09-10T06:45`，相差 8 小时。
- 本轮手工改为 `2026-09-10T14:45` 后提交成功。
- 影响：用户若直接使用默认值，会把业务事实时间记录早 8 小时。

## 服务恢复与最终续测记录

### ENV-004：默认 Java 与现有 Redis 均不能直接支撑项目启动

- 第一次直接启动后端时，系统默认 Java 为 8，运行 Java 21 编译产物失败（class file version 61，当前运行时仅支持到 52）。本轮改为显式使用仓库 `runtime/jdk` 后启动成功。
- 既有 `6379` Redis 进程仍在监听，但 Auth 日志稳定记录 `Redis command timed out`，Gateway 也在两秒会话校验超时后返回失败。该实例同时绑定 `0.0.0.0/[::]`，不符合 `runtime/README.md` 的自动复用条件。
- 依照仓库约束，未停止或修改既有 `6379` 进程；另在 `127.0.0.1:6380` 启动项目专用 Redis，并以 `REDIS_PORT=6380`、Java 21 重启 Gateway、Auth、Core、IoT。
- 最终监听：前端 5173、Redis 6380、Auth 10002、Core 10003、IoT 10004、Gateway 20001。`redis-cli -p 6380 PING` 返回 `PONG`，后续全部业务步骤在该运行环境完成。

### UI-008：登录成功后的首次受保护页跳转曾错误进入 403

- `mes.inspector` 登录成功并加载了 `pur:quality:inspect` 权限后，第一次从带 redirect 的登录页进入采购质检页，跳转到 `/forbidden`。
- 403 页显示所需权限 `pur:quality:inspect,pur:receipt:view`，但把当前角色显示为“普通用户”；重新登录后直接访问 `/purchasing/quality` 可以正常进入。
- 判断：间歇性首跳或前端权限上下文加载时序问题。未持续阻断续测，但会误导用户认为角色未授权。

### UI-009：两个成功弹窗的“前往办理”按钮未完成导航

- 质量处置执行成功后点击“前往上架任务”，页面标题短暂变为“入库上架任务”，随后仍停留在 `/purchasing/quality`；从左侧菜单可正常进入 `/purchasing/putaway`。
- 工序报工成功后点击“前往办理”，仍停留在 `/mes/executions`；从左侧菜单进入工单详情后可继续生产质检。
- 影响：主流程存在两处断续，需要用户知道备用菜单路径。

### UI-010：上架任务无法辨认采购、物料和目标库位

- 本轮上架任务 `PUT-0b865efc-e551-41c6-bc33-f6688773eb02` 的采购订单号、物料编码和物料名称显示 `-` 或空值；移动目标显示原始 UUID `36f97dbd-01b2-4108-af0d-b5cad2bff05c`。
- “确认执行货物上架”弹窗中的物料信息显示 `()`，来源暂存位为空，批次为 `-`；目标下拉仍可选择 `LOC-STOR-20260910-UI01`，因此本轮成功上架 100 件。
- 影响：执行动作可完成，但仓库人员无法从任务及确认弹窗核对正确业务对象。

### UI-011：派工与执行页面的业务名称和报工累计回显不一致

- 派工单 `DISP-f7bf7b34-e2ea-4949-8986-f92ace94c028` 将本轮成品显示为固定文案“定制产品”，工序显示“#待返回”；工单详情中工序显示 `(#)`。
- 新建执行实例时派工说明显示 `null`，操作员和工单主要显示 UUID；报工弹窗标题中的工序名称为空。
- 执行实例 `EXEC-46eda625-7398-43fc-a116-fad212a32b5b` 提交报工 `RPT-cff0ea52-a389-4144-b5bd-17d08ae38ca0`（合格 100、不良 0）后，执行列表的“累计报工”仍为 0；同一工单列表和详情正确显示报工 100。
- 影响：执行事实已经保存，但不同页面投影不一致，现场人员会误判报工未成功。

### UI-012：IoT 页面无法建立首个设备，浏览器闭环被模型前置数据阻断

- `iot.engineer` 打开设备台账时列表为空；点击“新建设备”后，“所属设备模型 (Profile)”必填下拉显示“无数据”。
- IoT 菜单只有“设备列表与详情”和“设备告警中心”，没有设备模型维护入口；告警中心也为空，且没有页面侧遥测或告警模拟入口。
- 本轮工艺路线与派工只能使用“人工通用工位”，执行实例的“机台设备 ID”仍是自由文本，无法从设备目录选择。
- 影响：无法仅通过浏览器创建设备、上报遥测、触发告警并关联运行中的 `EXEC-46eda625-7398-43fc-a116-fad212a32b5b`。本轮未用 MQTT 命令或数据库造数绕过页面，因此 IoT 段未通过。

### UI-013：销售发货默认时间仍按 UTC，且暂存位显示为未返回

- 打开发货确认弹窗时本地约为 20:45，“发货时间”默认值为 `2026-09-10T12:45`，相差 8 小时；本轮手工改为 `2026-09-10T20:45` 后提交。
- 弹窗规则文案显示从“发货暂存位（未返回真实库位）”移出，订单详情也只显示“由仓库人员选择真实库位”，无法核对实际 ShippingStaging 库位。
- 影响：与 UI-007 相同，默认值会把发货事实时间记早 8 小时；同时缺失发货暂存库位的可审计显示。

### UI-014：实时库存台账主数据投影为空，无法按本轮 SKU 对账

- 销售发货完成后以仓库角色打开 `/inventory/balances`，15 条库存余额的 SKU、物料名称、仓库、库位和批次均显示 `-`，库位类型显示 `undefined`；数量和最后流水时间仍有值。
- 在关键字框输入 `FG-20260910-UI01` 并查询，列表没有可见变化，无法辨认哪一行属于本轮成品或原料。
- 页面顶部企业总实物库存为 105、预留为 0、QualityHold 为 0；该总数包含历史测试数据，不能据此单独证明本轮原料扣减和成品发货后的逐 SKU 余额。
- 影响：各业务页面已返回领料扣减、成品入库增加和销售发货成功，但最终库存台账无法用业务编码交叉核对闭环余额。

## 本轮关键业务事实

| 环节 | 页面产物与最终状态 |
| --- | --- |
| 销售 | `SO-16f9229f-97fc-4b3b-8863-bce15866da23`，100 件，已完成 / 全部发货 |
| 生产 | `WO-9800b5c2-0085-4848-a554-7c77a93448e5`，报工 100、入库 100，已完工结案 |
| 采购 | `PO-afe78c33-26de-4be2-a613-bb588a54993b`，独立采购 100 件并完成到货 |
| 采购质检 | `QI-821bc445-b5b6-4e5b-945a-1bb83bbffa1c`，100 件全部合格；`QD-8435c6cc-13c4-475c-94b9-385d516a897d` 已由仓库执行 |
| 上架 | `PUT-0b865efc-e551-41c6-bc33-f6688773eb02`，100 件已进入 `LOC-STOR-20260910-UI01` |
| 生产领料 | `MI-idemp-c7e636cd-15e4-446b-9400-aa60add49279`，原料 100 件已扣减；流水 `907bd277-3b13-4774-9982-dcf858ed1f11` |
| 派工执行 | `DISP-f7bf7b34-e2ea-4949-8986-f92ace94c028`、`EXEC-46eda625-7398-43fc-a116-fad212a32b5b`，已下达并完工 |
| 报工与生产质检 | `RPT-cff0ea52-a389-4144-b5bd-17d08ae38ca0`，合格 100；`INS-097867` 已合格 |
| 成品入库 | `FGR-ce75f2ae-b578-42a2-bcc7-d61751f542af`，100 件已确认；流水 `d98d5a9b-56e6-4207-820f-278a25d6e827` |

## 最终追溯验证

- 从已完成销售订单点击“全链路追溯”，仍跳转到 `/traceability?entry_type=SALES_ORDER&entity_id=d1e4361c-1ae7-4335-a265-5f18c324d1de`。
- 页面再次稳定显示 `GIS_QUERY_002 请求的静态资源或接口不存在: api/traceability`，证明 UI-004 在全部业务动作完成后仍存在。
- 因销售行未关联工单、原料采购未关联工单、IoT 事实缺失且追溯接口不可用，本轮不能把销售状态“已完成”解释为完整黄金业务闭环验收通过。

## 2026-09-11 非环境问题处理记录

本次不处理 `ENV-001`～`ENV-004`。以下状态表示代码与迁移已完成并通过静态或自动化验证；由于运行中的前后端未在本次修改后重启，均保留“待浏览器回归”，不把代码检查替代为页面验收。

| 编号 | 处理状态 | 修改结果与回归重点 |
| --- | --- | --- |
| DOC-001 | 已校正 | 黄金流程明确为 `mes.inspector` 创建成品入库草稿、`warehouse.operator` 确认入库。 |
| DOC-002 | 已补主要入口 | 工单创建改用已审核销售订单行选择器；采购来源允许关联工单产出品或锁定 BOM 的组件物料；IoT 与追溯仍按各自能力条件验收。 |
| UI-001 | 代码已修复，待浏览器回归 | 库位列表加载仓库目录并按 `warehouseId` 回显仓库名称。 |
| UI-002 | 代码已修复，待浏览器回归 | 新建仓库下拉移除一期排除的 `WIP` 类型。 |
| UI-003 | 代码已修复，待浏览器回归 | 来源销售行改为真实订单行选择器，并为 `mes.inspector` 增加 `sales:order:view` 只读权限；不授予销售写权限。 |
| UI-004 | 改判为条件能力 | 规格与代码均将追溯/GIS 聚合限定在 `core.facts.iot.enabled` 及 Core↔IoT HMAC 配置齐备后装配；当前错误不能通过伪造空追溯接口修复，本次按用户要求不处理环境配置。 |
| UI-005 | 代码已修复，待浏览器回归 | 采购来源校验支持工单锁定 BOM 的组件原料，同时保留租户、删除状态和物料范围校验。 |
| UI-006 | 代码已修复，待浏览器回归 | 采购单、收货候选、质检事实与人员字段补充供应商、仓库、SKU、物料名称和用户名称投影，缺少目录数据时才退回标识符。 |
| UI-007 | 代码已修复，待浏览器回归 | 到货时间默认值改为浏览器本地 `datetime-local`，提交时再转换为 ISO 时间。 |
| UI-008 | 代码已修复，待浏览器回归 | 登录流程必须成功取得用户资料后才加载菜单和进入受保护路由，避免资料请求失败后以空角色继续首跳。 |
| UI-009 | 代码已修复，待浏览器回归 | 质量处置和工序报工成功后的目标路由改为等待 `router.push` 完成。 |
| UI-010 | 代码已修复，待浏览器回归 | 上架任务补充采购单、收货单、SKU、物料名称、单位、批次及真实来源/目标库位投影。 |
| UI-011 | 代码已修复，待浏览器回归 | 派工与执行列表关联工单、工艺、物料、设备、人员目录；累计报工按执行实例的报工事实汇总；新建执行实例改用设备目录选择。 |
| UI-012 | 代码已修复，待迁移及浏览器回归 | 新增设备模型与遥测菜单授权；设备为空模型时可直接进入模型维护；遥测页现有 HTTP QoS 1 模拟器可产生遥测并触发阈值告警。 |
| UI-013 | 代码已修复，待浏览器回归 | 发货时间默认值改为本地时间；打开弹窗前加载并回显真实 `ShippingStaging` 库位。 |
| UI-014 | 代码已修复，待浏览器回归 | 库存余额和流水展开后端维度对象，关联物料、仓库、库位目录，并对关键字与库位类型执行实际筛选。 |

### 已执行验证

- 前端：`npx vue-tsc --noEmit` 通过。
- Core：`ManufacturingFoundationApplicationServiceTest` 与 `PurchasingApplicationServiceTest` 共 18 个测试通过，包含 BOM 组件原料可作为采购来源的实现层用例。
- Auth：`AuthMigrationScriptTest` 共 9 个测试通过，包含 V14 IoT 导航与工单来源只读权限校验。
- 差异检查：`git diff --check` 通过。
- `npm run build` 的 `vue-tsc` 阶段通过，Vite 在写入正在由开发服务器占用的 `frontend/src/components.d.ts` 时返回 Windows `UNKNOWN open`；这是并行运行进程的文件占用，未据此新增业务缺陷，也未为本次处理停止现有服务。
