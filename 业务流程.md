## 一、角色登录方式

每次切换角色：

1. 点击右上角“退出登录”。
2. 在登录页点击对应的快捷登录按钮。
3. 点击“登录进入系统 POST /api/auth/login”。

角色与登录按钮：

| 业务角色     | 登录页按钮                   |
| ------------ | ---------------------------- |
| 租户管理员   | `租户管理员 admin.zhang`     |
| 销售人员     | `销售人员 sales.liu`         |
| 采购人员     | `采购人员 buyer.chen`        |
| 仓库人员     | `仓库人员 wh.operator`       |
| 生产质检人员 | `生产质检人员 mes.inspector` |

## 二、创建基础资料

### 第1步：创建计量单位

角色：租户管理员

1. 点击左侧“主数据管理 ▼”。
2. 点击“商品主数据”。
3. 点击页签或按钮“计量单位”。
4. 点击“＋ 新建计量单位”。
5. 填写：
   - `单位编码 (Code) *`
   - `单位名称 *`
   - `显示符号 (Symbol)`
   - `小数位数 (0-6)`
   - `状态`：选择 `ACTIVE (启用)`
   - `备注说明`
6. 点击“确认保存”。
7. 确认列表中能看到备注说明。

建议示例：

- 单位编码：`PCS-20260910-01`
- 单位名称：`测试件`
- 显示符号：`件`
- 小数位数：`0`
- 备注说明：`黄金流程测试单位`

### 第2步：创建仓库

角色：租户管理员

1. 在“商品主数据”页面点击“仓库主数据”。
2. 点击“＋ 新建仓库主数据”。
3. 填写：
   - `仓库编码 *`
   - `仓库名称 *`
   - `仓库类型 *`：选择 `STORAGE (综合存储仓)`
   - `负责人`
   - `联系电话`
   - `状态`：选择 `ACTIVE (启用)`
   - `仓库物理地址`
   - `备注说明`
4. 点击“确认保存”。

### 第3步：创建六类标准库位

角色：租户管理员

1. 点击“6类标准库位”。
2. 点击“＋ 新建6类标准库位”。
3. 填写：
   - `库位编码 *`
   - `库位名称 *`
   - `所属仓库 *`
   - `标准库位类型 *`
   - `库位容量`
   - `状态`：选择 `ACTIVE (启用)`
   - `规则描述与用途`
4. 按下面六类分别创建六个库位：
   - `ReceivingStaging (采购收货暂存位)`
   - `Storage (常规存储位)`
   - `Picking (拣货备料位)`
   - `ShippingStaging (发货暂存位)`
   - `QualityHold (质量隔离位)`
   - `Adjustment (差异调整位)`
5. 每个库位创建后都点击“确认保存”。

### 第4步：创建客户

角色：租户管理员

1. 点击“往来客户档案”。
2. 点击“＋ 新建往来客户档案”。
3. 在“新建客户档案”中填写：
   - `客户编码 *`
   - `客户名称 *`
   - `联系人`
   - `联系电话`
   - `地址信息`
4. 点击“确认保存”。

### 第5步：创建供应商

角色：租户管理员

1. 点击“往来供应商档案”。
2. 点击“＋ 新建往来供应商档案”。
3. 在“新建供应商档案”中填写：
   - `供应商编码 *`
   - `供应商名称 *`
   - `联系人`
   - `联系电话`
   - `地址信息`
4. 点击“确认保存”。

### 第6步：创建商品物料

角色：租户管理员

建议先创建一个原材料，再创建一个产成品。

1. 点击“＋ 新建商品物料主数据”。
2. 在“新建商品物料”中填写：
   - `物料编码 (SKU) *`
   - `物料名称 *`
   - `规格型号`
   - `计量单位 (UOM) *`
   - `物料分类`
   - `参考单价 (元)`
   - `最低库存预警值`
   - `安全库存量`
   - 是否勾选 `启用批次管理 (Lot Management)`
   - `备注说明`
3. 原材料的`物料分类`选择 `原材料`。
4. 产成品的`物料分类`选择 `产成品`。
5. 点击“确认保存”。

如果“计量单位 (UOM)”或其他下拉框没有内容，先返回确认计量单位已经保存并且状态是 `ACTIVE (启用)`。

## 三、创建BOM和工艺路线

### 第7步：创建BOM

角色：生产质检人员

1. 点击左侧“制造执行 ▼”。
2. 点击“BOM 物料清单”。
3. 点击“＋新建物料清单”。
4. 在“新建物料清单 (BOM)”中填写：
   - `BOM 编码 *`
   - `版本号 *`
   - `产出产品 *`
   - `状态 *`：选择 `生效中 (ACTIVE，可供工单选择)`
   - `BOM 原料明细项 *`
5. 点击“＋ 添加物料”。
6. 在明细行中填写或选择：
   - `选择真实组件产品`
   - `数量 (如 1.00)`
   - `单位 (PCS)`
   - `损耗 (如 0.01)`
7. 点击“保存 BOM”。

注意：如果后续创建工单时“关联 BOM”没有选项，先回到BOM页面确认状态为 `生效中 (ACTIVE，可供工单选择)`。

### 第8步：创建工艺路线

角色：生产质检人员

1. 点击“工艺路线管理”。
2. 点击“＋新建工艺路线”。
3. 在“新建工艺路线 (Routing)”中填写：
   - `工艺路线编码 *`
   - `版本号 *`
   - `产出产品 *`
   - `状态 *`：选择 `生效中 (ACTIVE，可供工单选择)`
   - `工序步骤定义 *`
4. 点击“＋ 添加工序”。
5. 填写：
   - `序号`
   - `工序名称 (如 主板贴片)`
   - 选择 `人工通用工位（无需专用设备）`
   - `标准工时(分)`
6. 点击“保存工艺路线”。

## 四、销售下单和审核

### 第9步：创建销售订单

角色：销售人员

1. 点击左侧“销售出库 ▼”。
2. 点击“销售订单”。
3. 点击“＋ 新建销售订单”。
4. 在“新建销售订单”中填写：
   - `往来客户 *`
   - `计划发货日期 *`
   - `订购物料 *`
   - `订购数量 *`
   - `发运备注`
5. 点击“生成销售订单”。

### 第10步：提交并审核销售订单

角色：销售人员

1. 在销售列表找到刚创建的订单。
2. 点击操作列中的“详情 / 履约”。
3. 在详情抽屉中点击“提交订单”。
4. 如出现确认窗口，点击“确认”。
5. 再次点击“详情 / 履约”。
6. 点击“审核通过 (进入履约)”。
7. 如出现确认窗口，点击“确认”。

正常结果：

- 生命周期显示为 `履约中 (Approved)`
- 履约进度仍可能显示 `尚未开始 (NotStarted)`

“审核通过”后显示“履约中”是正常的，不是“审核中”。

## 五、创建并下达生产工单

### 第11步：创建生产工单

角色：生产质检人员

1. 点击“制造执行 ▼”。

2. 点击“生产工单”。

3. 点击“＋新建生产工单”。

4. 在“新建生产工单 (Work Order)”中填写：

   - 可以先在`目录搜索`输入产品编码，然后点击“搜索”
   - `产出产品 *`
   - `计划生产数量 *`
   - `计划开工时间 *`
   - `计划完工时间 *`
   - `关联 BOM *`
   - `关联工艺路线 *`
   - `来源销售订单行 ID (可选，仅用于追溯)`

5. 日期字段直接通过页面日期时间控件选择。

6. 不要填写类似：

   `2026-09-01T18:00:00+08:00`

7. 页面提示应为：

   `页面选择日期和时间，提交时自动转换为服务端时间格式。`

8. 如果不知道真实的销售订单行 ID，`来源销售订单行 ID (可选，仅用于追溯)`可以留空。

9. 点击“确认创建工单 (Draft)”。

### 第12步：提交并批准生产工单

角色：生产质检人员

1. 在工单列表找到状态为 `草稿 (Draft)` 的工单。
2. 点击“提交”。
3. 在确认窗口点击“确认”。
4. 等状态变为 `待审核 (PendingApproval)`。
5. 点击“批准”。
6. 在确认窗口点击“确认”。

正常结果：

- 工单状态变为 `已下达 (Released)`。

只有状态为 `已下达 (Released)`的工单，后续“新建工序派工安排”中的工单下拉框才会有内容。

## 六、创建采购订单并审核

### 第13步：创建采购订单

角色：采购人员

1. 点击“采购入库 ▼”。
2. 点击“采购订单”。
3. 点击“＋ 新建采购订单”。
4. 在“新建采购订单”中填写：
   - `供应商 *`
   - `目标仓库 *`
   - `计划到货日期 *`
   - `采购商品物料 *`
   - `采购数量 *`
   - `关联来源工单 (可选，用于追溯)`
5. 如果要关联生产工单，在`关联来源工单 (可选，用于追溯)`中选择刚才创建的工单。
6. 点击“确认生成采购单”。

### 第14步：提交并审核采购订单

角色：采购人员

1. 在采购列表找到刚创建的采购订单。
2. 点击“详情”。
3. 在详情抽屉中点击“提交订单”。
4. 确认窗口点击“确认”。
5. 再次点击“详情”。
6. 点击“审核通过”。
7. 确认窗口点击“确认”。

正常结果：

- 采购订单状态变为 `已审核 (Approved)`。

## 七、采购到货验收

### 第15步：接收采购到货

角色：仓库人员

1. 点击“采购入库 ▼”。

2. 点击“采购到货验收”。

3. 在筛选条件中选择或查看 `已审核 (Approved)`。

4. 点击“查询”。

5. 找到对应采购订单。

6. 点击操作列的“验收接收”。

7. 在“仓库到货外观验收与接收”中填写：

   - `到货验收时间 *`
   - `入库隔离库位 (QualityHold) *`
   - `本次到货量 *`
   - `外观拒收量`
   - `批次编号`
   - 如果拒收量大于0，填写`拒收原因 (拒收>0必填)`

8. 确认：

   `实际接收量 = 本次到货量 - 外观拒收量`

9. 点击“确认接收进质量隔离位”。

接收成功后，系统会提示是否前往采购质检。建议点击确认跳转采购质检页面，不要手工复制或填写UUID。

## 八、采购质检和质量处置

### 第16步：录入采购质检结果

角色：生产质检人员

1. 点击“采购入库 ▼”。
2. 点击“采购质检与处置”。
3. 如果是从到货验收成功提示跳转过来的，页面应自动带出采购订单、收货凭证和收货行。
4. 点击“＋ 录入到货质检结果”。
5. 在“录入采购到货质检结果”中填写：
   - `检验总数量 *`
   - `质检合格数量 *`
   - `质检不合格数量 (自动计算)`
   - `检验说明与备注`
6. 点击“确认提交检验事实”。

不要在没有收货上下文的情况下手工填采购订单ID、采购订单行ID或随机UUID。页面可能提示：

```
质检提交已阻止：必须从真实收货确认响应取得独立 receiptId 和收货行 ID；采购订单行 ID、订单 ID 或随机 UUID 不能代用。
```

### 第17步：下达质量处置决定

角色：生产质检人员

1. 在“采购质检与处置”页面点击页签“质量处置决定与执行”。
2. 找到刚完成检验的记录。
3. 点击“做出处置决定”。
4. 在“做出质量处置决定”中填写：
   - `处置动作类型 *`：选择 `合格放行上架 (Release ➔ 移至收货暂存位)`
   - `处置数量 *`
   - `处置依据与原因`
5. 点击“确认下达处置决定”。

## 九、执行质量处置和上架

### 第18步：执行合格放行

角色：仓库人员

1. 点击“采购入库 ▼”。
2. 点击“采购质检与处置”。
3. 点击页签“质量处置决定与执行”。
4. 找到状态为待仓库执行的记录。
5. 点击“确认执行实物处置”。
6. 在确认窗口填写：
   - `放行至收货暂存位 *`：选择 `ReceivingStaging` 类型库位
   - `后续上架目标库位（可选）`：可以选择目标库位，也可以保留 `由后端选择默认 Storage 库位`
7. 点击“确认”。

### 第19步：确认上架

角色：仓库人员

1. 点击“采购入库 ▼”。
2. 点击“上架任务”。
3. 在筛选中选择 `待上架 (Pending)`。
4. 点击“查询”。
5. 找到待上架任务。
6. 点击“确认上架”。
7. 在“确认执行货物上架”中填写：
   - `目标常规存储库位 (Storage) *`
   - `本次上架数量 *`
8. 点击“确认移入目标库位”。

正常结果：

- 上架状态变为 `已确认完成 (Confirmed)`
- 操作列显示 `已入库`

## 十、生产领料

### 第20步：创建生产领料申请

角色：生产质检人员

1. 点击“制造执行 ▼”。
2. 点击“生产领料与成品入库”。
3. 页面标题实际显示为“生产领料与退料 (Material Movement)”。
4. 点击“＋新建领料申请单”。
5. 在“新建生产领料申请单”中填写：
   - `关联生产工单 *`
   - `领用物料 *`
   - `领料数量 *`
   - `出库仓库 *`
   - `出库库位 *`
   - `超领原因 (可选，超出定额时必填说明)`
6. 点击“保存领料单 (草稿)`。

这里需要特别注意：当前页面保存后，列表读取仍存在缺口。如果保存成功后“生产领料单 (Material Issues)”列表仍为空，看不到“出库确认”，请停在这里，不要手工填写UUID。该项目前应记录为阻断问题。

## 十一、创建派工单

### 第21步：创建派工安排

角色：生产质检人员

1. 点击“制造执行 ▼”。
2. 点击“车间派工看板”。
3. 点击“＋新建派工单”。
4. 在“新建工序派工安排”中填写：
   - `关联已下达工单 *`：选择状态为 `已下达 (Released)`的生产工单
   - `指派工序步骤 *`
   - `责任操作工 *`
   - `派工指派数量 *`
   - `指定加工设备 (可选，未指定为人工通用工位)`
5. `指派工序步骤 *`必须先选择工单后才会出现内容。
6. `责任操作工 *`选择实际操作员，例如页面中显示的`U1005 - 李质检`。
7. 点击“保存派工单 (Draft)”。
8. 在派工列表找到刚创建的草稿。
9. 点击“下达”。
10. 确认窗口点击“确认”。

正常结果：

- 派工状态变为 `已下达 (Released)`。

如果`关联已下达工单 *`没有选项，先返回“生产工单”，确认工单状态确实是 `已下达 (Released)`。如果已经是 `生产中 (InProgress)`，该下拉框没有内容是正常的，因为不能重复创建派工。

## 十二、创建工序执行并报工

### 第22步：创建工序执行实例

角色：生产质检人员

1. 点击“制造执行 ▼”。
2. 点击“工序报工与质检”。
3. 点击“＋发起工序执行”。
4. 在“创建工序实际执行实例”中填写：
   - `关联已下达派工单 (Released) *`
   - `机台设备 ID (可选)`
5. 点击“保存执行实例”。

### 第23步：开始和完成工序

角色：生产质检人员

1. 在执行列表找到状态为 `未开始 (NotStarted)`的记录。
2. 点击“开始”。
3. 确认窗口点击“确认”。
4. 等状态变为 `正在加工 (Running)`。
5. 点击“完成”。
6. 确认窗口点击“确认”。

如需测试暂停功能，才使用：

- `暂停`
- 在“工序暂停确认”中填写`暂停原因 (如换料、设备维护、交接班) *`
- 点击“确认暂停”
- 之后点击“恢复”

正常黄金流程不需要暂停。

### 第24步：提交工序报工

角色：生产质检人员

1. 在执行列表找到状态为 `已完成 (Completed)`的记录。
2. 点击“报工”。
3. 在“工序报工录入”中填写：
   - `申报合格数量 *`
   - `申报不良数量 *`
   - `报工时间`
   - `备注说明`
4. 点击“提交报工”。

## 十三、生产质检

### 第25步：发起生产质检

角色：生产质检人员

1. 回到“生产工单”。
2. 找到对应工单，点击“详情”。
3. 点击页签“生产质检”。
4. 在“1. 关联现场工序报工记录 (Work Reports)”区域找到报工记录。
5. 点击“发起质检”。
6. 在“发起生产质检”中填写：
   - `关联报工单`
   - `质检编号`
   - `检验类型 *`：选择 `完工总检 (Final Inspection)`
   - `抽样数量 *`
7. 点击“保存质检单 (Draft)”。

### 第26步：录入生产质检结论

角色：生产质检人员

1. 在质检记录操作列点击“录入结果”。
2. 在“评定质检结论”中填写：
   - `检验结论判定 *`：选择 `合格放行 (Passed)`
   - `合格数量 *`
   - `不良品数量 *`
3. 点击“提交结论”。

## 十四、成品入库

### 第27步：创建成品入库单

角色：生产质检人员

1. 点击“制造执行 ▼”。

2. 点击“成品完工入库”。

3. 点击“＋新建成品入库单”。

4. 在“新建成品入库申请单”中填写：

   - `生产工单 *`
   - `成品入库数量 *`
   - `目标入库仓库 *`
   - `目标货架库位 *`

5. 选择工单后，系统会显示：

   `当前最大可申报入库量为 ...`

6. 入库数量不能超过该最大数量。

7. 点击“保存入库单 (草稿)`。

### 第28步：确认成品入库

角色：生产质检人员

1. 在成品入库列表找到草稿记录。
2. 点击“确认入库”。
3. 在“确认成品入库”窗口点击“确认”。

正常结果：

- 状态显示为 `已确认入库 (Confirmed)`
- 操作显示为 `已完成增加`

### 第29步：完成生产工单

角色：生产质检人员

1. 回到“生产工单”。
2. 点击对应工单的“详情”。
3. 点击“工单完工”。
4. 在确认窗口点击“确认”。

不要优先使用“手工结案”。只有正常生产流程无法完成、需要受控终止时，才使用“手工结案”。

## 十五、销售拣货和发货

### 第30步：直接拣货

角色：仓库人员

1. 点击“销售出库 ▼”。
2. 点击“直接拣货与发货”。
3. 在筛选中选择 `已审核 (Approved)`和`未开始 (NotStarted)`。
4. 点击“查询”。
5. 找到对应销售订单。
6. 点击“直接拣货”。
7. 在“执行销售直接拣货”中填写：
   - `来源库位 *`：选择产成品所在的`Storage`库位
   - `本次拣货数量 *`
8. 点击“确认拣货”。

如果没有“来源库位”选项，先确认成品入库已经完成，并且库存确实进入了`Storage (常规存储位)`。

### 第31步：确认销售发货

角色：仓库人员

1. 在销售订单中点击“履约详情”或“详情 / 履约”。
2. 点击“确认发货 (扣减实物库存)”。
3. 在“销售发货确认与库存扣减”中填写：
   - `发货时间 *`
   - `承运物流商 *`
   - `物流运单号`
   - `本次发货数量 *`
4. `承运物流商 *`可选择：
   - `顺丰冷链物流`
   - `跨越速运`
   - `德邦精准汽运`
   - `客户自提 (专车直运)`
5. 点击“确认发货并扣减实物库存”。

最终正常结果：

- 销售订单生命周期：`已完成 (Completed)`
- 履约进度：`全部发货 (FullyShipped)`